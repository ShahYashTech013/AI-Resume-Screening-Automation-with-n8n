name: Run n8n Automation

on:
  push:
    branches: [ "main" ]

jobs:
  trigger:
    runs-on: ubuntu-latest

    steps:
    - name: Trigger n8n Workflow
      run: curl -X POST https://your-n8n-url/webhook/ai-resume
